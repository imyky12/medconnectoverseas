import { Router } from 'express';

const router = Router();

// GET /api/v1/admin/dashboard/stats    — Revenue, user count, purchases
// GET /api/v1/admin/dashboard/revenue  — Revenue breakdown
// Routes will be implemented during the admin dashboard feature

export default router;
